import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container:{
        // flex: 0.25
    },
    pic:{
        width: 60,
        height: 60,
        borderRadius: 50,
        marginRight: 15,
    }
});

export default styles;